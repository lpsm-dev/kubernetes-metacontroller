---
apiVersion: homelabz.com.br/v1
kind: PodService
metadata:
  name: nginx
spec:
  image: nginx:stable-alpine
  port: 80
  cpuReq: 50m
  cpuLimit: 100m
  memReq: 64Mi
  memLimit: 128m
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: podservice-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: podservice.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx
                port:
                  number: 80
